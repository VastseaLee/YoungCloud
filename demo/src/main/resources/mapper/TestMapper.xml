<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.young.demo.dao.TestDao">

    <select id="hello" parameterType="long" resultType="com.young.demo.bean.User">
        SELECT ${table} FROM  WHERE id = #{id}
    </select>

</mapper>